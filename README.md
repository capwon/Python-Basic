# Python-Basic

## 2026-09-13 업데이트
- 알파벳 리스트를 만들고 역순으로 출력하는 방법
- `input().split()`과 `map()`으로 공백으로 구분된 정수를 리스트에 저장하는 방법
- 입력받은 리스트에서 특정 위치의 값을 선택하는 방법
- 반별 평균 점수에서 두 반의 점수를 찾아 합산하는 방법
- 여러 점수의 총점과 평균을 `sum()`과 `len()`으로 계산하는 방법
- 리스트의 홀수는 2배, 짝수는 2로 나누어 출력하는 방법
- 리스트의 마지막에 입력된 종료값 `0`을 제외하고 처리하는 방법
- 숙제로 풀 수 있도록 `hw.py`에 최댓값·최솟값과 정렬 문제 추가

## 2026-09-20 업데이트
- `cos_pro_3.py`에 `solution(nums)` 함수를 정의하고 리스트에서 최댓값을 반환하는 코드 추가
- `hw.py`에 solution 함수를 사용하는 예제 문제 추가